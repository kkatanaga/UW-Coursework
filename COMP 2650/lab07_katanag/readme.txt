main.c was compiled using gcc (MinGW) in Eclipse; main.exe should run by itself (open a command prompt) without problems.

In the case of main.exe not working, compile* main.c from Eclipse by:

Opening main.c as a project in Eclipse and running it from the IDE.

*this is assuming the user can compile C programs on their system.