The program was written and compiled using Replit
When I ran the same program on my personal desktop, the getenv("USER") and getenv("PWD") both output (null), 
	so I could only show the result from Replit

You can run hello.exe as an executable, or run it through the terminal with ./hello or hello